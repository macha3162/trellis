-content_for :modal_title do
  = best_in_place [@board, @list, @card], :title, as: :input

.row
  .col-md-8
    = "リスト:#{@list.name}"
    %br
    = best_in_place [@board, @list, @card], :description, as: :textarea, place_holder: '説明を編集', display_with: :simple_format
  .col-md-4
    .page-header
      %h5
        アクション
    = link_to fa_icon('trash') + ' 削除', board_list_card_path(@board,@list,@card), method: :delete, class: 'btn btn-default btn-block'
