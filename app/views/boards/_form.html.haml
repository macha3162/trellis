= simple_form_for(@board) do |f|
  = f.error_notification

  .form-inputs
    = f.input :name, placeholder: '例えば「XXXXプロジェクト管理」など'

  .form-actions
    = link_to 'Back', boards_path, class: 'btn btn-default'
    = f.button :submit
